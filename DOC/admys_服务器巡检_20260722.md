# admys.cn 服务器日常监测与巡检（2026-07-22）

| 项目 | 内容 |
| ------ | ------ |
| 文档类型 | 生产服务器只读巡检摘要 |
| 巡检对象 | `root@8.163.51.48`（阿里云 ECS，`ssh yashe`） |
| 关联站点 | `https://admys.cn`、`https://api.admys.cn` |
| 巡检时间 | 2026-07-22 12:05—12:10 CST |
| 巡检方式 | SSH 只读检查（systemctl / journalctl / nginx 日志 / 本机 curl / 公网端口探测） |
| 文档版本 | V1.0 |
| 是否改动服务器 | 否（纯只读） |
| 前置事项 | 本机 SSH 密钥轮换后，新公钥已写入服务器 `authorized_keys`，登录恢复正常 |

## 修订历史

| 日期 | 版本 | 修订内容 | 修订人 |
| ------ | ------ | ---------- | -------- |
| 2026-07-22 | V1.0 | 日常监测：服务健康、资源、Nginx 当日日志、公网端口、证书有效期 | 运维巡检 |

---

## 1  执行摘要

本次只读巡检确认：**核心业务服务全部在线，未见 5xx，可用性良好。**

主要结论：

1. **正常**：`nginx` / `yashe-api` / `mysqld` / `redis` 均为 `active` + `enabled`；API 仍仅监听 `127.0.0.1:8080`；本机 HTTPS（`admys.cn` / `api.admys.cn`）返回 `200`。
2. **当日流量偏扫描**：07-22 访问以 404 扫描为主（`/.env`、`nmap`、`actuator` 等），**几乎无真实 `/api/*` 业务请求**；最近一次明显后台业务流量在 07-20。
3. **遗留风险（中，与既往一致）**：MySQL / 宝塔面板进程仍监听 `0.0.0.0`（安全组已挡住外网）；`kdump.service` 失败不影响业务；SSH 失败尝试仍多。
4. **证书**：`api.admys.cn` 证书有效至 **2026-10-15**，目前正常。

综合判断：**线上可继续使用；无需紧急处置。** OPS-02/03（MySQL、面板监听收紧）仍建议择机处理。

---

## 2  主机与资源

| 指标 | 观测值 | 评价 |
| ------ | ------ | ------ |
| 主机名 | `iZ7xv7lm9b9ir1qe71sxwuZ` | — |
| 系统时间 | 2026-07-22 12:05 CST | 正常 |
| 运行时长 | 约 5 天 19 小时 | 正常（自 07-16 起） |
| 负载 | `0.02, 0.02, 0.00` | 很低 |
| 根分区 | 40G，已用 7.7G（21%），可用约 30G | 正常 |
| Inode | 已用约 6% | 正常 |
| 内存 | 总计 1.8Gi；可用约 1.1Gi；已用约 723Mi | 可接受 |
| Swap | 已用约 332Mi / 1.0Gi | 有占用，暂不紧急 |
| `yashe-api` 内存 | 约 194–204 MiB | 正常 |

结论：资源充裕，当前流量下无压力。

---

## 3  服务健康

| 服务 | Active | 开机自启 | 监听地址 | 说明 |
| ------ | -------- | -------- | -------- | ------ |
| `yashe-api.service` | running | enabled | `127.0.0.1:8080` | 自 2026-07-20 14:32 起持续运行 |
| `nginx.service` | running | enabled | `0.0.0.0:80/443` | 自 07-20 08:23 起持续运行 |
| `mysqld.service` | running | enabled | `*:3306`、`*:33060` | 进程全网卡监听；外网不可达 |
| `redis.service` | running | enabled | `127.0.0.1:6379` | 绑定本机正确 |
| 宝塔 `BT-Panel` | running | — | `0.0.0.0:8888` | 进程全网卡监听；外网不可达 |
| `kdump.service` | **failed** | — | — | 崩溃转储配置失败，不影响业务 |

### 3.1  本机探活（服务器内 curl）

| 目标 | HTTP 码 | 说明 |
| ------ | -------- | ------ |
| `http://127.0.0.1:8080/` | 401 | API 在线，未认证被拒绝（预期） |
| `https://127.0.0.1/` + Host `api.admys.cn` | 200 | Nginx → API 站点正常 |
| `https://127.0.0.1/` + Host `admys.cn` | 200 | 主站入口正常 |

### 3.2  公网端口探测（巡检机发起）

| 端口 | 结果 | 说明 |
| ------ | ------ | ------ |
| 22 | 可达 | SSH |
| 80 | 可达 | HTTP |
| 443 | 可达 | HTTPS |
| 3306 | 不可达 | 安全组有效拦截 |
| 8888 | 不可达 | 安全组有效拦截 |

### 3.3  TLS 证书（api.admys.cn）

| 项 | 值 |
| ---- | ---- |
| Subject | `CN=api.admys.cn` |
| 生效 | 2026-07-17 |
| 到期 | **2026-10-15** |
| 剩余 | 约 85 天 | 建议 9 月中旬前确认自动续期是否正常 |

### 3.4  API 日志（24h）

- `journalctl -u yashe-api -p warning --since=-24h`：**无条目**
- `journalctl -u yashe-api -p err --since=-24h`：**无条目**

说明：07-20 启动日志中仍有 Spring Security「开发用随机密码」类 WARN（历史启动输出）。若生产仍启用默认 UserDetails 自动配置，建议后续代码侧确认已关闭无关自动配置；**本次未改服务器、未复述任何密钥内容。**

---

## 4  Nginx 访问与错误日志（2026-07-22）

日志范围：

- 当前：`/var/log/nginx/access.log`（轮转后今日片段，约 90 行）
- 轮转：`/var/log/nginx/access.log-20260722`（约至 03:18 的当日早段）

### 4.1  状态码概况（当日两份 access 合计，约）

| 状态码 | 次数（约） | 含义 |
| ------ | ---------- | ------ |
| 404 | 564 | 扫描与不存在路径为主 |
| 200 | 68 | 正常响应（含静态/首页类） |
| 400 | 42 | 非法/不完整请求 |
| 405 | 12 | 方法不允许 |
| 301 | 3 | 重定向 |
| 401 | 3 | 未认证（扫描打到 `/api/*` 敏感路径） |
| 5xx | **0** | **未见服务端错误** |

另有少量畸形 TLS/二进制探测导致的异常状态字段，可忽略。

### 4.2  高频 404 路径（节选）

| 路径类型 | 示例 | 解释 |
| ---------- | ------ | ------ |
| 根路径 / 静态缺失 | `/`、`/robots.txt`、`/sitemap.xml`、`/favicon.ico` | 爬虫与扫描；API 主机无对应静态文件属预期噪音 |
| 凭据/配置扫描 | `/.env`、`/credentials.json`、`/application.yml`、`/actuator/env` | 未命中真实敏感文件 |
| 漏洞探针 | `/SDK/webLanguage`、`/owa/auth/logon.aspx`、`/HNAP1`、`nmaplowercheck*` | 公网扫描 |
| 业务假路径 | `/api/.env`、`/api/env`、`/api/config` | 401/404，鉴权或路由未放开 |

### 4.3  业务 API 观察

- **07-22 当日**：几乎无真实业务 `/api/auth/*`、`/api/admin/*` 成功访问记录。
- **07-20**：有后台登录与管理接口成功流量（`/api/auth/login`、`/api/auth/me`、`/api/admin/stats`、`/api/admin/members` 等均为 200）。
- **07-21**：仅见扫描类 `/api/session/properties` → 404。

结论：服务健康，但近两日生产业务访问很少；若预期有日常运营流量，需业务侧另行确认是否正常。

### 4.4  error.log 要点（07-22）

1. 大量 `No such file or directory`：目标为 `robots.txt`、`favicon.ico`、`sitemap.xml`、扫描路径等 → **扫描噪音**。
2. 轮转错误日志中可见 `access forbidden by rule`（约 56 次）→ Nginx 拦截规则仍在生效。
3. 当前 `error.log` 片段以静态文件缺失为主，**未见 upstream 超时或 502/504 类业务故障**。

---

## 5  安全与运维观察

| 编号 | 级别 | 现象 | 建议 |
| ------ | ------ | ------ | ------ |
| OPS-02 | 中 | MySQL 监听 `*:3306` | 改为 `127.0.0.1` 或仅内网（安全组已挡，仍建议纵深防御） |
| OPS-03 | 中 | 宝塔面板监听 `0.0.0.0:8888` | 仅本机/白名单，或关闭公网面板入口 |
| OPS-04 | 低 | `kdump.service` failed | 可择机修复或禁用，不影响业务 |
| OPS-05 | 低 | 近 24h SSH 失败类日志约 482 条 | 常态爆破噪音；保持仅密钥登录、考虑 fail2ban/限源 |
| OPS-06 | 低 | 证书 10-15 到期 | 关注自动续期；到期前 30 天再检 |
| OPS-07 | 信息 | 07-22 业务 API 几乎无访问 | 确认是否符合预期运营节奏 |

SSH 运维钥匙：今日本机轮换后新公钥已写入；`authorized_keys` 当前约 4 行。离职或密钥泄露时应及时删旧公钥。

---

## 6  对照上次巡检（07-19 / 07-20）

| 项 | 上次 | 本次（07-22） |
| ---- | ------ | ---------------- |
| 四核心服务 | 均 active | 均 active |
| Nginx enable | 07-20 已完成 | 仍为 enabled |
| 5xx | 0 | 0 |
| 资源 | 磁盘 ~21%，负载极低 | 基本同水平 |
| OPS-01 Nginx 自启 | 已关闭 | 保持关闭 |
| OPS-02/03 监听收紧 | 未处理 | **仍未处理** |
| 业务流量 | 07-19/20 有后台访问 | 07-21/22 以扫描为主 |

---

## 7  结论与下一步

**结论：生产可用，无紧急故障。**

建议优先级：

1. （中）择机收紧 MySQL / 宝塔监听地址（OPS-02/03）。
2. （低）关注证书续期与 SSH 爆破噪音治理。
3. （信息）若业务侧预期每日有后台访问，核对 07-21 起流量骤降是否正常。

本次巡检**未对服务器做任何配置变更或重启**。
