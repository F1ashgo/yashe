server:
  port: 8080
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yashe_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: dengshanzu
    password: wsad824600
    driver-class-name: com.mysql.cj.jdbc.Driver

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.yashe.entity
  configuration:
    map-underscore-to-camel-case: true

# JWT
jwt:
  secret: YasheAtelierDesMiyabi2024SecretKeyForJWTTokenGeneration
  expiration: 604800000  # 7 days in ms
