<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yashe.mapper.ReviewMapper">
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO reviews (user_id, project, rating, content) VALUES (#{userId}, #{project}, #{rating}, #{content})
  </insert>
  <select id="findByUserId" resultType="com.yashe.entity.Review">
    SELECT * FROM reviews WHERE user_id = #{userId} ORDER BY created_at DESC
  </select>
</mapper>
